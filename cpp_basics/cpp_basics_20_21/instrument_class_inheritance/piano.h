#include "instrument.h"

class Piano : public Instrument
{
public:
  // constructor and destructor
  Piano(std::string name, std::string sound);
  ~Piano();

  // getters and setters

  // methods
private:
};
